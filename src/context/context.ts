import React from "react";
import {HomepageContextType, MatrixContextType} from '../types/types'

export const HomepageContext = React.createContext({} as HomepageContextType)
export const MatrixContext = React.createContext({} as MatrixContextType)